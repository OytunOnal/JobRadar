node_modules/
.next/
tsconfig.tsbuildinfo
out/
*.log
.env
.env.local
prisma/dev.db
prisma/dev.db-journal
next-env.d.ts
data/

# Personal data — never commit (each user keeps their own, gitignored)
config/user.ts
config/profile.generated.json
config/cv.txt
application-kit.md
job-search-sites.md

sweep-state.json
sweep.log
fit-fill.log
fit-review.log
desc-fill.log
eliminated-sample.txt
eliminated-v2.txt
data/embed-cache/
docs/embed-eval-results.md
migrate-layered.log
embed-fill.log
prisma/dev.db-shm
prisma/dev.db-wal
prisma/dev.db.pre-layered-backup
migrate-layered.log
embed-fill.log
config/settings.json
# ...and any backup of it. A settings.json.bak slipped into a commit because
# the pattern named one exact file: the app writes .bak beside its config, so
# the ignore has to cover what the app actually produces, not one filename.
config/settings.json.*
config/*.bak
.run/
